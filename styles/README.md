# /styles Folder

This folder holds the global CSS for **J GIL Brothers**.

## Files

- `styles.css` – main stylesheet loaded on every page.

## How to use

In your `index.html` (and any future HTML files), include:

```html
<link rel="stylesheet" href="/styles/styles.css" />
